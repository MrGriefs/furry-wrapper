import { Options, QueryReturns } from './Base';
/**
 *
 * @param options - Library options
 * @returns - A random (sfw) image
 */
declare function FB(options?: Options): QueryReturns;
export default FB;
